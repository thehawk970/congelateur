{% extends 'base.html.twig' %}

{% block title %}Food index{% endblock %}

{% block body %}
        <a href="{{ path('app_food_new') }}" class="btn btn-outline-success">Create new</a>
    {% if food|length > 0 %}
    <table id="datatable" class="table table-bordered dt-responsive  nowrap w-100">
        <thead>
            <tr>
                <th>Catégory</th>
                <th>Quantity</th>
                <th>Label</th>
                <th>Weight</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for food in food %}
            <tr>
                <td>{{ component('Tag', {
                        color: food.category.color,
                        food: food
                    }) }}</td>
                <td>{{ food.quantity }}</td>
                <td>{{ food.label }}</td>
                <td>{{ food.weight }}</td>
                <td>
                    <a href="{{ path('app_food_show', {'id': food.id}) }}" class="btn btn-outline-info">🔎 Voir</a>
                    <a href="{{ path('app_food_edit', {'id': food.id}) }}" class="btn btn-outline-warning">📝 <E></E>dit</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    {% else %}
        <div class="alert alert-primary mt-5" role="alert">
            Pas de nourriture encore enregistrée
        </div>
    {% endif %}


{% endblock %}
